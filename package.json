{
    "name": "fuel-rats-v3",
    "packageManager": "yarn@3.2.0",
    "private": true,
    "scripts": {
        "[-] Deployment": "",
        "windows": "yarn build && altv-server.exe",
        "linux": "yarn build && ./start.sh",
        "dev": "nodemon --watch \"./src/**\" --ext \"ts\" --exec \"yarn build && altv-server.exe\"",
        "[-] Build": "",
        "build": "yarn clean && yarn build:client && yarn build:server && yarn build:webview && yarn copy:mods && yarn copy:configs",
        "build:client": "yarn workspace @fuelrats/client build",
        "build:server": "yarn workspace @fuelrats/server build",
        "build:webview": "yarn workspace @fuelrats/webview build",
        "[-] Copy Files": "",
        "copy:mods": "yarn workspace @fuelrats/mods copy",
        "copy:configs": "yarn workspace @fuelrats/configs copy",
        "[-] Development": "",
        "vue-dev": "yarn workspace @fuelrats/webview dev",
        "[-] Updates": "",
        "update": "altv-pkg d release",
        "[-] Utilities": "",
        "clean": "rimraf resources"
    },
    "workspaces": [
        "src/*"
    ],
    "devDependencies": {
        "altv-pkg": "^1.2.0",
        "esbuild": "^0.14.34",
        "prettier": "^2.6.2",
        "typescript": "^4.6.3"
    },
    "dependencies": {
        "nodemon": "^2.0.15",
        "rimraf": "^3.0.2"
    },
    "type": "module"
}
